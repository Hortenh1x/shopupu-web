node_modules
.next
out
dist
.env.local
.env*.local
# real env files stay on the machine that runs the app: the server's copy carries
# NEXT_PUBLIC_GOOGLE_CLIENT_ID, and a tracked one would overwrite it on deploy
.env.production
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
tsconfig.tsbuildinfo

# Playwright artifacts
test-results/
playwright-report/

# Local tooling / IDE / agent session data
.claude/
.idea/
